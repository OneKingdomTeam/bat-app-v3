venv/
__pycache__

# Database files - SQLite with WAL mode
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
*.sqlite-shm
*.sqlite-wal

# Persistent data directory (mounted volume) - all persistent files
persistent/

# Legacy paths (no longer used but kept for safety)
app/uploads/*
app/data/db/
app/data/uploads/
